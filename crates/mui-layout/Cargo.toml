[package]
name = "mui-layout"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# The spacing vocabulary, shared with the style crate.
mui-geometry = { path = "../mui-geometry" }
# Hot maps keyed by addresses and hashes, not attacker input.
rustc-hash = "1.1"
