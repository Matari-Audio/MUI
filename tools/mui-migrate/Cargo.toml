[package]
name = "mui-migrate"
version = "0.1.0"
edition = "2024"
publish = false
description = "Token-accurate rewriter that migrates code to the MUI DSL v2 API."

# Standalone: not a member of the main MUI workspace.
[workspace]

[dependencies]
proc-macro2 = { version = "1.0.107", features = ["span-locations"] }
similar = "3.2.0"
toml_edit = "0.25.15"
